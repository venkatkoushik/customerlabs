export const Circle = ({ color }) => (
    <div style={{
        width: "10px",
        height: "10px",
        borderRadius: "100%",
        backgroundColor: color,
        display: "inline-block"
    }} />
)